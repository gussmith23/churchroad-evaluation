# Environment script for Churchroad evaluation. Source this before developing on
# or running the evaluation.

########################## DO NOT EDIT BELOW THIS LINE #########################

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

# Set up PYTHONPATH for eval.
export PYTHONPATH="$SCRIPT_DIR/python"
